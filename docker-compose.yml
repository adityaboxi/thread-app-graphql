services:
  thread-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: thread-app
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped